# Definir la lista de números
numeros = (1, 2, 3, 4, 6, 10, 11)

# Iterar sobre cada número en la lista
for numero in numeros:
    if numero % 2 == 0:
        print(f"Esto es par: {numero}")
    else:
        print(f"Esto no es par: {numero}")


